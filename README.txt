Game introduction:

6 walled box with 3 balls that bounce off walls/other balls at different speeds and in different directions.


To play the game:

Download the GitLab repository and unzip the files. Clone the godot cpp submodule by running the following in the terminal in the project folder: "git clone -b 4.1 https://github.com/godotengine/godot-cpp". Download scons by running the following commands: "python3 -m venv ~/venv", "source ~/venv/bin/activate", "pip install scons". Compile the code with the command "scons" in the directory with the SConstruct file. Open the godot project manager software by running "godot" command in the terminal. Click scan in the right hand side navigation bar and choose path to the Project folder. Open the project and click the play button to run the game.


Acknowledgements:

Ball mesh - https://polyhaven.com/a/patterned_brick_floor
Wall mesh - https://polyhaven.com/a/wood_floor_deck